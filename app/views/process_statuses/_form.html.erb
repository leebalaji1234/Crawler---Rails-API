<%= form_for(@process_status) do |f| %>
  <% if @process_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@process_status.errors.count, "error") %> prohibited this process_status from being saved:</h2>

      <ul>
      <% @process_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :process_status %><br>
    <%= f.text_field :process_status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
