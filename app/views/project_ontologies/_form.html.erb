<%= form_for(@project_ontology) do |f| %>
  <% if @project_ontology.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_ontology.errors.count, "error") %> prohibited this project_ontology from being saved:</h2>

      <ul>
      <% @project_ontology.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ontology_id %><br>
    <%= f.number_field :ontology_id %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br>
    <%= f.text_field :project_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
