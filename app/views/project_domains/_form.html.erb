<%= form_for(@project_domain) do |f| %>
  <% if @project_domain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_domain.errors.count, "error") %> prohibited this project_domain from being saved:</h2>

      <ul>
      <% @project_domain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :domain_id %><br>
    <%= f.number_field :domain_id %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br>
    <%= f.text_field :project_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
